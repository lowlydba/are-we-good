{
  "name": "are-we-good",
  "version": "0.0.0",
  "description": "Rolls up multiple GitHub Actions job statuses into a single status check",
  "main": "dist/main.js",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --platform=node --target=node24 --format=esm --minify --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --alias:undici=./src/stub-undici.js --outfile=dist/main.js",
    "test": "node --test --experimental-test-coverage src/index.test.ts",
    "format": "prettier --write \"src/**/*.ts\" \"*.json\" \".github/**/*.yml\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"*.json\" \".github/**/*.yml\""
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "@octokit/webhooks-types": "^7.6.1",
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.2",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2"
  }
}
